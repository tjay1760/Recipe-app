    <%= stylesheet_link_tag "food", "data-turbo-track": "reload" %>
        <%= stylesheet_link_tag "recipe", "data-turbo-track": "reload" %>

<div class='no-recipes'>
  <%= "No recipes available." if @recipes.empty? %>
</div>

<div class='add-recipe'>
  <%= link_to "Add Recipe", new_recipe_path, class: "btn-add-recipe btn-addfood" %>
</div>

<% if @recipes.any? %>
  <ul class="recipe-list">
    <% @recipes.each do |recipe| %>
      <li>
        <div class="recipe-info">
          <div class="recipe-name">
            <%= link_to recipe.name, recipe_path(recipe), class: "recipe-name" %>
          </div>
          <div class="recipe-actions">
  <% if current_user == recipe.user %>
    <%= link_to "Remove", recipe_path(recipe), method: :delete, data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this recipe?" }, class: "btn-remove-recipe" %>
  <% end %>
</div>

        </div>
        <div class="recipe-description">
          <%= recipe.description %>
        </div>
      </li>
    <% end %>
  </ul>
<% end %>